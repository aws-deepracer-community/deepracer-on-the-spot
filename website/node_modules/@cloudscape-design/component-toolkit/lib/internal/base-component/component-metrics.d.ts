interface Settings {
    packageSource: string;
    packageVersion: string;
    theme: string;
}
export declare function useComponentMetrics(componentName: string, { packageSource, packageVersion, theme }: Settings): void;
export {};
